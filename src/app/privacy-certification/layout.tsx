import { privacyCertificationMetadata as metadata } from '@/lib/metadata'
export { metadata }

export default function PrivacyCertificationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
